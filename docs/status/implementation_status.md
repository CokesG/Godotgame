# Dead Man's Ante Implementation Status

Last updated: 2026-05-12

This file is the handoff ledger for future agents. Update it whenever a phase changes state.

Allowed statuses:
- `TODO`: not started
- `IN_PROGRESS`: currently being implemented
- `DONE`: implemented and verified
- `BLOCKED`: cannot proceed; include blocker and owner/action

## Current Project Facts

- Project path: `C:\Game Dev\CodexGame`
- Engine target: Godot 4.6.x
- MCP: Godot AI at `http://127.0.0.1:8000/mcp`
- Design source: `docs/design/dead_mans_ante_mega_prompt.md`
- Prototype title: Dead Man's Ante
- First class: The Gambler-Knight
- First board: shared 3x3 Control-based grid

## Phase Ledger

| Phase | Status | Acceptance Criteria | Evidence |
| --- | --- | --- | --- |
| 0 - Foundation Lock | DONE | Folders, status tracker, resource scripts, and empty test scene exist; project opens; `TestCombat.tscn` runs without script errors. | Added folder scaffold, `CardDefinition.gd`, `EnemyDefinition.gd`, `IntentDefinition.gd`, `TestCombat.tscn`; script checks for all kickoff scripts reported `SCRIPT_CHECKS: PASS`; `Godot_v4.6.2-stable_win64_console.exe --headless --path "C:\Game Dev\CodexGame" --quit` exited 0; `--scene res://scenes/combat/TestCombat.tscn --quit-after 1` exited 0. |
| 1 - Combat State Machine | DONE | Turn manager cycles through all eight phases repeatedly; phase label and combat log update; reset works. | Added `TurnManager.gd` and `TestCombatController.gd`; `tests/debug/turn_manager_phase_check.gd` reported `TURN_MANAGER_PHASE_CHECK: PASS`; MCP `scene_open` opened `res://scenes/combat/TestCombat.tscn`; MCP `editor_state` reported `current_scene: res://scenes/combat/TestCombat.tscn` and `readiness: ready`. |
| 2 - 3x3 Tactical Grid | DONE | Board cells, player token, enemy token, occupancy, selection, and invalid-move logging work. | Added `GridCellView.gd`, `CombatGrid.gd`, and `tests/debug/combat_grid_check.gd`; `PHASE2_SCRIPT_CHECKS: PASS`; `COMBAT_GRID_CHECK: PASS`; `PHASE2_BEHAVIOR_CHECKS: PASS`; headless project and `TestCombat.tscn` scene loads exited 0; MCP `scene_open` opened `res://scenes/combat/TestCombat.tscn`; MCP `editor_state` reported `project_name: Dead Man's Ante`, `current_scene: res://scenes/combat/TestCombat.tscn`, and `readiness: ready`. |
| 3 - Card Data And Deck Loop | DONE | Deck starts combat, draws 5 cards, plays cards to discard/exhaust, and shuffles discard when needed. | Added `DeckManager.gd`, `CardView.gd`, `HandView.gd`, `DeckManagerCheck.tscn`, `deck_manager_check.gd`, and 10 starter card resources under `resources/cards`; TestCombat now resets deck and draws 5 on combat reset; clicking a card plays it to discard or exhaust; `PHASE3_SCRIPT_CHECKS: PASS`; `DECK_MANAGER_CHECK: PASS`; `TURN_MANAGER_PHASE_CHECK: PASS`; `COMBAT_GRID_CHECK: PASS`; headless project and TestCombat scene loads exited 0; MCP `scene_open` opened `res://scenes/combat/TestCombat.tscn`; MCP `editor_state` reported `project_name: Dead Man's Ante`, `current_scene: res://scenes/combat/TestCombat.tscn`, and `readiness: ready`. |
| 4 - Enemy Intent Ranges | DONE | Enemy shows weighted intent preview, hidden intent resolves during reveal, and debug truth display works. | Added `EnemyIntentSystem.gd`, 9 intent resources under `resources/intents`, 3 enemy resources under `resources/enemies`, `EnemyIntentSystemCheck.tscn`, and `enemy_intent_system_check.gd`; TestCombat now shows public weighted intent previews and a toggleable debug truth panel; Enemy Intent Preview phase rolls hidden intents and Reveal phase reveals them; `PHASE4_SCRIPT_CHECKS: PASS`; `ENEMY_INTENT_SYSTEM_CHECK: PASS`; `PHASE4_BEHAVIOR_CHECKS: PASS`; `DECK_MANAGER_CHECK: PASS`; `TURN_MANAGER_PHASE_CHECK: PASS`; `COMBAT_GRID_CHECK: PASS`; headless project and TestCombat scene loads exited 0; MCP `scene_open` opened `res://scenes/combat/TestCombat.tscn`; MCP `editor_state` reported `project_name: Dead Man's Ante`, `current_scene: res://scenes/combat/TestCombat.tscn`, and `readiness: ready`. |
| 5 - Commit / Bluff / Reveal | DONE | Player can commit, call, fold, raise, wager Nerve, and see clear payoff/penalty logs. | Added `BluffSystem.gd`, `BluffSystemCheck.tscn`, and `bluff_system_check.gd`; `DeckManager.gd` now supports a committed card zone plus fold/resolve; TestCombat now has Commit First, Set Call, Raise +1, Fold, Reset Bluff, enemy/intent/lane call dropdowns, and Nerve/wager/committed-card state display; Reveal resolves the hidden intent against the Call and resolves the committed card; `ALL_GAMEPLAY_SCRIPT_CHECKS: PASS (17 scripts)`; `BLUFF_SYSTEM_CHECK: PASS`; `ENEMY_INTENT_SYSTEM_CHECK: PASS`; `DECK_MANAGER_CHECK: PASS`; `TURN_MANAGER_PHASE_CHECK: PASS`; `COMBAT_GRID_CHECK: PASS`; headless project and TestCombat scene loads exited 0. |
| 6 - Combat Resolver | DONE | Cards and revealed enemy intents affect HP, Guard, movement/trap logs, and win/loss state. | Added `CombatResolver.gd`, `CombatResolverCheck.tscn`, and `combat_resolver_check.gd`; TestCombat now shows Combat State, applies clicked/committed card effects, applies revealed enemy intent payloads, tracks player/enemy HP and Guard, logs movement/trap placeholders, and emits victory/defeat outcomes; `ALL_GAMEPLAY_SCRIPT_CHECKS: PASS (19 scripts)`; `COMBAT_RESOLVER_CHECK: PASS`; `BLUFF_SYSTEM_CHECK: PASS`; `ENEMY_INTENT_SYSTEM_CHECK: PASS`; `DECK_MANAGER_CHECK: PASS`; `TURN_MANAGER_PHASE_CHECK: PASS`; `COMBAT_GRID_CHECK: PASS`; headless project and TestCombat scene loads exited 0. |
| 7 - Playable Combat Loop Integration | DONE | The TestCombat harness gates actions by phase, refills/spends Energy, draws to hand target, resolves Commit/Bluff/Reveal automatically in the Reveal phase, cleans up each turn, disables actions after win/loss, and has an integrated debug check proving the loop. | Added `CombatSession.gd`, `PlayableLoopCheck.tscn`, and `playable_loop_check.gd`; `DeckManager.gd` now exposes hand-card lookup and allows normal plays while a committed card is pending; TestCombat now shows Energy, gates cards to Player Commit, gates Call/Raise/Fold to Bluff Wager, auto-resolves Reveal once, discards remaining hand during Cleanup, and locks actions after combat outcome; `PLAYABLE_LOOP_CHECK: PASS`; `PHASE7_SCENE_CHECKS: PASS`; `PHASE7_SCRIPT_STYLE_CHECKS: PASS`; `PHASE7_LOAD_CHECKS: PASS`; 21 gameplay/debug scripts present. |
| 8 - Playability UX Pass | DONE | TestCombat has clear primary controls, phase-specific guidance, a compact playable combat recipe, debug controls hidden behind a toggle, and verification that the playable UI elements exist and load cleanly. | Added primary Continue/New Combat/Show Debug controls, `TurnGuidance`, `RecipePanel`, phase-specific guidance copy, recipe progress tracking, hidden debug controls, hidden debug truth by default, and `TestCombatUICheck.tscn` / `test_combat_ui_check.gd`; `TEST_COMBAT_UI_CHECK: PASS`; `PHASE8_SCENE_CHECKS: PASS`; `PHASE8_SCRIPT_STYLE_CHECKS: PASS`; `PHASE8_LOAD_CHECKS: PASS`; 22 gameplay/debug scripts present. |
| 9 - Targeted Tactical Card Actions | DONE | Attack cards can target a selected living enemy, movement cards can consume the selected grid move target, the grid represents the three prototype enemies, committed cards preserve their selected target context, and the behavior is covered by a debug check. | Added `TargetEnemyOption`, `MovementCellOption`, targeted card context in TestCombat, committed-card target memory, `CombatResolver.apply_card_with_context`, alive enemy target queries, top-row grid spawns for Brute/Skulker/Shieldbearer, and `TargetedCardActionCheck.tscn` / `targeted_card_action_check.gd`; `TARGETED_CARD_ACTION_CHECK: PASS`; `TEST_COMBAT_UI_CHECK: PASS`; `PHASE9_SCENE_CHECKS: PASS`; `PHASE9_SCRIPT_STYLE_CHECKS: PASS`; `PHASE9_LOAD_CHECKS: PASS`; 23 gameplay/debug scripts present. |
| 10 - Positional Intent And Reveal Feel | DONE | Enemy intent damage respects player lane, tracking attacks still hit, correct calls visibly mitigate matching damage, traps are stored by cell/lane and can punish revealed intents, and reveal logs clearly explain hits, misses, mitigations, and trap triggers. | Added lane/player/unit snapshot helpers to `CombatGrid.gd`, structured last-reveal fields to `BluffSystem.gd`, `CombatResolver.apply_revealed_intents_with_context`, lane hit/miss logic, tracking attacks, correct-call damage mitigation, trap cells with lane triggers, lane text in intent previews, aligned first-fight intent copy/lanes, and `PositionalIntentCheck.tscn` / `positional_intent_check.gd`; `POSITIONAL_INTENT_CHECK: PASS`; `PHASE10_SCENE_CHECKS: PASS`; `PHASE10_SCRIPT_STYLE_CHECKS: PASS`; `PHASE10_LOAD_CHECKS: PASS`; 24 gameplay/debug scripts present. |
| 11 - Prototype Run Slice | DONE | TestCombat can play a short five-encounter run with persistent deck/HP state, post-combat card rewards, elite relic rewards, dynamic encounters, 20 card resources, 5 normal enemies, 1 elite, 1 boss, and rough balance sanity checks. | Added `RunManager.gd`, `RelicDefinition.gd`, 10 new card resources, 14 new intent resources, Needle-Eye, Hexmonger, Grave Dealer elite, House Champion boss, 5 relic resources, run/reward UI in TestCombat, persistent run deck/Blood, relic combat modifiers, and `Phase11RunSliceCheck.tscn` / `phase11_run_slice_check.gd`; MCP `project_run` on `Phase11RunSliceCheck.tscn` reported `PHASE11_RUN_SLICE_CHECK: PASS`; MCP `project_run` launched `TestCombat.tscn` with no editor errors and a valid game framebuffer; full headless debug scene sweep reported PASS for BluffSystem, CombatResolver, DeckManager, EnemyIntentSystem, Phase11RunSlice, PlayableLoop, PositionalIntent, TargetedCardAction, and TestCombatUI; headless project and TestCombat scene loads exited 0; 27 gameplay/debug scripts present. |
| 12 - Run Balance And Playtest Tooling | DONE | TestCombat shows a balance report, fast run preview, and run results summary; reward offers are score-sorted against encounter tags and deck gaps; a simulator can estimate deck power, encounter threat, projected turns, survival margin, expected damage, and predicted run clears. | Added `RunBalanceSimulator.gd`, reward scoring in `RunManager.gd`, run stat/result tracking, balance and results panels in TestCombat, and `Phase12BalanceToolingCheck.tscn` / `phase12_balance_tooling_check.gd`; MCP `project_run` on `Phase12BalanceToolingCheck.tscn` reported `PHASE12_BALANCE_TOOLING_CHECK: PASS`; MCP `project_run` launched `TestCombat.tscn` with no editor errors and a valid game framebuffer; full headless debug scene sweep reported PASS for BluffSystem, CombatResolver, DeckManager, EnemyIntentSystem, Phase11RunSlice, Phase12BalanceTooling, PlayableLoop, PositionalIntent, TargetedCardAction, and TestCombatUI; headless project and TestCombat scene loads exited 0. |
| 13 - Playtest Tuning And Run Export | DONE | TestCombat can run five strategy playtest simulations, card/enemy/intent tuning keeps at least three of five sims clearing, reward recommendations explain deck gaps, danger/close/favorable balance bands are obvious, and run summaries can be exported for comparison. | Tuned card damage/Guard values, Brute/Needle-Eye/Grave Dealer/House Champion HP and intent weights, added strategy playtest batches, reward explanation details, visible balance band labels/colors, `Run 5 Sims` and `Export Summary` debug controls, JSON run export, and `Phase13PlaytestTuningCheck.tscn` / `phase13_playtest_tuning_check.gd`; `Phase13PlaytestTuningCheck.tscn` reported `PHASE13_PLAYTEST_TUNING_CHECK: PASS`; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; MCP `project_run` on `Phase13PlaytestTuningCheck.tscn` reported `PHASE13_PLAYTEST_TUNING_CHECK: PASS`; MCP launched `TestCombat.tscn` with game capture available and no editor errors. |
| 14 - Run UX Readability Pass | DONE | TestCombat presents the run as a playable prototype instead of a harness, with a clear run header, next-action prompt, top-threat intent summary, readable reward prompt, and verification that debug controls remain tucked away. | Updated TestCombat title/subtitle, added `RunHeader`, `ActionPrompt`, `ThreatSummary`, and `RewardPrompt`, made intent previews mark each enemy's top threat, added top-threat response copy, improved reward button sizing, and added `Phase14RunUxCheck.tscn` / `phase14_run_ux_check.gd`; `Phase14RunUxCheck.tscn` reported `PHASE14_RUN_UX_CHECK: PASS`; `TestCombatUICheck.tscn` reported `TEST_COMBAT_UI_CHECK: PASS`; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; MCP `project_run` on `Phase14RunUxCheck.tscn` reported `PHASE14_RUN_UX_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 960x540 game framebuffer and no editor errors. |
| 15 - Run Shell Flow | DONE | TestCombat has a player-facing start shell, gates combat until Start Run, switches to a reward shell after combat victory, advances cleanly to the next table after rewards, and shows a results shell with New Run and Export Summary actions after run end. | Added `RunShellPanel`, `RunShellTitle`, `RunShellDetail`, `StartRunButton`, `ShellNewRunButton`, and `ShellExportButton`, added run-flow states for start/combat/reward/results, blocked combat advancement until Start Run, synchronized shell state from `RunManager`, and added `Phase15RunShellCheck.tscn` / `phase15_run_shell_check.gd`; `Phase15RunShellCheck.tscn` reported `PHASE15_RUN_SHELL_CHECK: PASS`; `Phase14RunUxCheck.tscn` and `TestCombatUICheck.tscn` still pass; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; MCP `project_run` on `Phase15RunShellCheck.tscn` reported `PHASE15_RUN_SHELL_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 960x540 game framebuffer and no editor errors. |
| 16 - Debug Drawer Cleanup | DONE | The default TestCombat play view hides raw diagnostics while preserving a single toggleable debug drawer for recipe steps, run state, balance projections, playtest sims, debug controls, and truth display. | Added `DebugDrawer` and `DebugSummary`, moved raw debug controls into the drawer, hid `RecipePanel`, `RunState`, `BalanceReport`, and `PlaytestReport` by default, kept player-facing run shell/threat/reward UI visible, and added `Phase16DebugDrawerCheck.tscn` / `phase16_debug_drawer_check.gd`; `Phase16DebugDrawerCheck.tscn` reported `PHASE16_DEBUG_DRAWER_CHECK: PASS`; `Phase14RunUxCheck.tscn`, `Phase15RunShellCheck.tscn`, and `TestCombatUICheck.tscn` still pass; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; MCP `project_run` on `Phase16DebugDrawerCheck.tscn` reported `PHASE16_DEBUG_DRAWER_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 960x540 game framebuffer and no editor errors. |
| 17 - Combat Readability In Play Area | DONE | TestCombat explains turn state, card action affordances, enemy HP/threat, target/move context, and danger/close/favorable balance status inside the play area without relying on the log. | Added visible `TurnStatus`, `EnemyStatus`, and `CardActionHint` readouts, refreshed them from combat/session/run state, made target and move dropdown changes update card guidance, improved `CardView` text/tooltips with cost, type, target, tags, and type-colored borders, and added `Phase17CombatReadabilityCheck.tscn` / `phase17_combat_readability_check.gd`; `Phase17CombatReadabilityCheck.tscn`, `TestCombatUICheck.tscn`, `Phase14RunUxCheck.tscn`, `Phase15RunShellCheck.tscn`, and `Phase16DebugDrawerCheck.tscn` reported PASS; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; headless project and `TestCombat.tscn` scene loads exited 0; MCP `project_run` launched `TestCombat.tscn` with a 960x540 game framebuffer and no editor errors. |
| 18 - Combat Feel Feedback | DONE | TestCombat gives immediate combat feel beats for phase transitions, card play, committed cards, movement, Guard gain/loss, HP damage, traps, enemy defeats, and intent reveals without changing combat math. | Added `CombatFeedbackPanel`, `FeedbackBanner`, and `CombatFeedback` feed, pulse/tween feedback on relevant UI controls, resolver-state delta feedback for damage/Guard/traps/outcomes, grid `flash_unit` / `flash_cell` support, `GridCellView.play_feedback`, card/commit/call/raise/fold/reveal/move feedback hooks, and `Phase18CombatFeedbackCheck.tscn` / `phase18_combat_feedback_check.gd`; `Phase18CombatFeedbackCheck.tscn`, `TestCombatUICheck.tscn`, `Phase17CombatReadabilityCheck.tscn`, `Phase16DebugDrawerCheck.tscn`, and `Phase14RunUxCheck.tscn` reported PASS; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; headless project and `TestCombat.tscn` scene loads exited 0; MCP `project_run` on `Phase18CombatFeedbackCheck.tscn` reported `PHASE18_COMBAT_FEEDBACK_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 960x540 game framebuffer and no editor errors. |
| 19 - Combat Presentation Pass | DONE | TestCombat shows compact intent markers, hover-based card target/effect previews before click, highlighted previewed cards, grid target focus, and floating hit/Guard numbers over the play area. | Added `IntentIconStrip`, `CardTargetPreview`, card hover/unhover signals in `CardView`, preview relay/highlight support in `HandView`, card preview effect text and grid focus hooks in TestCombat, floating combat text methods in `CombatGrid`, and `Phase19CombatPresentationCheck.tscn` / `phase19_combat_presentation_check.gd`; `Phase19CombatPresentationCheck.tscn`, `TestCombatUICheck.tscn`, `Phase18CombatFeedbackCheck.tscn`, `Phase17CombatReadabilityCheck.tscn`, and `Phase14RunUxCheck.tscn` reported PASS; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; headless project and `TestCombat.tscn` scene loads exited 0; MCP `project_run` on `Phase19CombatPresentationCheck.tscn` reported `PHASE19_COMBAT_PRESENTATION_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 960x540 game framebuffer and no editor errors. |
| 20 - Playable Combat Loop Quality | DONE | The default TestCombat path uses a smart pacing button instead of raw phase stepping, keeps manual reset out of the default play path, and makes begin/resolve/next-turn flow feel game-like while preserving debug drawer controls. | Reworked `ContinueButton` into smart `Begin Turn`, `Resolve Turn`, `Reveal Turn`, and `Next Turn` states; auto-skips draw/read setup into `PLAYER_COMMIT`, auto-runs bluff/reveal into `RESOLVE`, auto-cleans/draws/reads next turn back into planning, hides `NewCombatButton` unless debug is open, updated loop guidance copy, and added `Phase20LoopQualityCheck.tscn` / `phase20_loop_quality_check.gd`; `Phase20LoopQualityCheck.tscn`, `Phase17CombatReadabilityCheck.tscn`, `Phase18CombatFeedbackCheck.tscn`, `Phase19CombatPresentationCheck.tscn`, and `Phase15RunShellCheck.tscn` reported PASS; full headless debug scene sweep reported `ALL_DEBUG_SCENES_PASS`; headless project and `TestCombat.tscn` scene loads exited 0; MCP `project_run` on `Phase20LoopQualityCheck.tscn` reported `PHASE20_LOOP_QUALITY_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 960x540 game framebuffer and no editor errors. |
| 21 - Run Continuity Pass | DONE | Combat victory/defeat flows cleanly through reward, next-encounter, and results screens so TestCombat feels like a continuous five-table run instead of a combat sandbox. | Added `RunContinuity` and `NextEncounterButton`, introduced a `next_encounter` run-flow state, made reward selection land on a clear `Next Table` preview instead of auto-starting combat, added upcoming encounter/enemy/balance context to the shell, kept defeat/victory results actions explicit, exposed current/next enemy names from `RunManager`, and added `Phase21RunContinuityCheck.tscn` / `phase21_run_continuity_check.gd`; `Phase21RunContinuityCheck.tscn`, `Phase15RunShellCheck.tscn`, `Phase20LoopQualityCheck.tscn`, `Phase14RunUxCheck.tscn`, `TestCombatUICheck.tscn`, `PlayableLoopCheck.tscn`, `PositionalIntentCheck.tscn`, and `TargetedCardActionCheck.tscn` reported PASS; a headless debug sweep passed through `Phase20LoopQualityCheck.tscn` before Windows process allocation failed on the next Godot launch with error 1455, then `Phase21RunContinuityCheck.tscn` and remaining gameplay checks were rerun separately and passed; headless project and `TestCombat.tscn` scene loads exited 0; MCP `project_run` on `Phase21RunContinuityCheck.tscn` reported `PHASE21_RUN_CONTINUITY_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 640x360 game framebuffer and no editor errors. |
| 22 - Encounter Pacing And Content | DONE | Each table has a distinct pre-combat identity with enemy introductions, a named table modifier, and clear reward stakes before combat starts. | Added encounter intro, table modifier, modifier payload, reward stakes, and reward tag labels to every `RunManager` node; exposed current table metadata and table modifiers through run state; added `EncounterPreview` to the run shell; made start and next-table screens show enemies, intro copy, modifier rules, reward stakes, and reward tags; combined table modifiers with relic modifiers for max Energy, hand size, starting Nerve, and starting Guard; and added `Phase22EncounterPacingCheck.tscn` / `phase22_encounter_pacing_check.gd`; `Phase22EncounterPacingCheck.tscn`, `Phase21RunContinuityCheck.tscn`, `Phase20LoopQualityCheck.tscn`, `Phase15RunShellCheck.tscn`, `TestCombatUICheck.tscn`, `Phase11RunSliceCheck.tscn`, `Phase12BalanceToolingCheck.tscn`, and `Phase13PlaytestTuningCheck.tscn` reported PASS; headless project and `TestCombat.tscn` scene loads exited 0; MCP `project_run` on `Phase22EncounterPacingCheck.tscn` reported `PHASE22_ENCOUNTER_PACING_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 640x360 game framebuffer and no editor errors. |
| 23 - Table Modifier Readability | DONE | The active table rule stays visible in the play area during combat, and setup effects visibly trigger through feedback beats instead of only being implied by changed numbers. | Added `TableRulePanel` / `TableRuleStatus`, active modifier effect text, table-rule trigger history, trigger feedback for Energy cap, hand size, starting Nerve, and opening Guard, and a final setup-surface pass so the latest table rule trigger remains in the feedback banner after turn reset; added `Phase23TableRuleReadabilityCheck.tscn` / `phase23_table_rule_readability_check.gd` and updated `TestCombatUICheck.tscn` coverage; `Phase23TableRuleReadabilityCheck.tscn`, `Phase22EncounterPacingCheck.tscn`, `Phase21RunContinuityCheck.tscn`, `Phase18CombatFeedbackCheck.tscn`, and `TestCombatUICheck.tscn` reported PASS; headless project and `TestCombat.tscn` scene loads exited 0; MCP `project_run` on `Phase23TableRuleReadabilityCheck.tscn` reported `PHASE23_TABLE_RULE_READABILITY_CHECK: PASS`; MCP launched `TestCombat.tscn` with a 640x360 game framebuffer and no editor errors. |

## Completion Rules

- Mark a phase `DONE` only after running a verification step and adding evidence.
- If a phase is partially built but unverified, keep it `IN_PROGRESS`.
- Keep final art, 3D, run map, relic economy, shops, meta progression, and complex animation out of scope until Phase 5 proves the hook.
